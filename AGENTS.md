# 维护约定

- 本仓库独立维护目录双语插件。只支持 Windows、Zotero 10.0.1–10.0.x、个人库和已有目录 PDF；不增加 OCR、目录生成、全文翻译、自动扫描或批量翻译。
- 保持插件 ID `bilingual-outline@lllateron`、偏好前缀 `extensions.bilingualOutline.`、本机数据目录 `bilingual-outline` 与现有 schema/笔记标识稳定。
- 与其他插件只共用系列父条目，不读写其笔记，不创建可见标签。设置同步和译文同步独立；密钥与同步开关仅本机保存。
- 同一 PDF 重译成功覆盖整份，失败保留旧数据。不同 PDF 并发修改保留；同一 PDF 冲突整份选择。
- 清理仅针对已永久删除的 PDF 条目，保留回收站和暂不可用文件；预览、备份和恢复语义不可削弱。
- 阅读器窗口数据先复制到插件作用域再处理；传入窗口的对象按需 cloneInto。保留异常路径中解除按钮忙碌状态的 finally。
- 不提交运行数据、密钥、依赖、构建产物、机器路径或开发流水。使用原生 SVG 图标风格，功能按钮不折叠。
- 开始任务前读取当前源码和 docs/DEVELOPMENT.md。从最新 main 创建 codex/<task> 分支，验证后通过 PR 合并；发布更新清单由工作流管理。
- 发布前运行 npm run verify，更新 CHANGELOG 与逐版本验收记录。模拟测试、源码检查和人工验收必须分别表述。
- 未经用户授权不访问真实文库、不调用付费翻译、不安装插件或发布版本。用户已授权的任务无需重复确认。
